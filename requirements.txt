# Torch
--find-links https://download.pytorch.org/whl/cu113
torch==1.12.0; sys_platform != 'darwin'
torchvision==0.13.0; sys_platform != 'darwin'
torchaudio==0.12.0; sys_platform != 'darwin'

torch==1.12.0; sys_platform == 'darwin'
torchvision==0.13.0; sys_platform == 'darwin'
torchaudio==0.12.0; sys_platform == 'darwin'

# Jax
--find-links https://storage.googleapis.com/jax-releases/jax_releases.html
jax[cuda11_cudnn805]; sys_platform != 'darwin'
jax[cpu]; sys_platform == 'darwin'

# Nvidia RAPIDS
# See https://rapids.ai/pip.html#install
--find-links https://developer.download.nvidia.com/compute/redist/cudf-cu11
--find-links https://developer.download.nvidia.com/compute/redist/dask-cudf-cu11
--find-links https://developer.download.nvidia.com/compute/redist/cuml-cu11
--find-links https://developer.download.nvidia.com/compute/redist/cugraph-cu11
cudf-cu11
dask-cudf-cu11
cuml-cu11
cugraph-cu11

# Ray
ray==1.13.0

# FFCV
ffcv
numba

# Huggingface
transformers
datasets[vision]

# Adversarial example things
torchattacks

# CLIP
ftfy
regex
git+https://github.com/openai/CLIP.git

# Other deep learning libraries
einops
mup
neural-tangents
pytorch-lightning
tensor-canvas
torchtyping

# Configuration parsing
simple-parsing
dacite

# ThunderSVM
/home/gridsan/groups/ccg/programs/thundersvm/python; sys_platform != 'darwin'

# UMAP
umap-learn

# Other computational libraries
matplotlib
pandas
plotly

# Experiment management
wandb

# Utilities
black
flake8
gpustat
isort
ipywidgets
jupyterlab
mypy
tqdm
mlxtend
